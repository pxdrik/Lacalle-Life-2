/**
 * Placeholder shell. The real home surface is designed in the design-system
 * stage; this exists only so the route tree is valid and the build is
 * verifiable from stage one.
 */
export default function HomePage() {
  return (
    <main>
      <h1>Lacalle Life</h1>
    </main>
  );
}
