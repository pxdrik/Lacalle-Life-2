# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# testing
/coverage

# typescript
*.tsbuildinfo

# misc
.DS_Store
*.pem
.vercel

# certificado autoassinado gerado sob demanda para testar em HTTPS na rede
# local, e o servidor descartável que o usa — nenhum dos dois é parte do app
.cert/
https-server-temp.mjs

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env
.env
.env*.local

# scratch files from the exercise-media scripts: an 846 KB download cache and
# the matcher's raw proposals. Both are regenerable and neither is a source of
# truth — the reviewed pairs live in scripts/build-exercise-media.mjs.
scripts/.cache-*.json
scripts/.matches.json
scripts/.cache-wger.json
